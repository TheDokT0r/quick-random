/**
 * A list of all avaliable primitive types that are supported by the library
 */
type PrimitiveTypeStrings =
    'number' | 'string' | 'boolean' | 'symbol' | 'date' | 'bigint'| 'unknown';

export default PrimitiveTypeStrings;